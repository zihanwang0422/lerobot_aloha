# LeRobot ACT

## 整体架构

```
主臂(Leader) × 2  →  bi_so_leader（遥操作器）
                         ↓ 关节角度
从臂(Follower) × 2 ←  bi_so_follower（机器人）
                         ↓ 录制
                     LeRobotDataset（数据集）
                         ↓ 训练
                     ACT Policy（双臂 Aloha）
```

动作维度：双臂共 **14 维** = 左臂 6 关节 + 1 夹爪 + 右臂 6 关节 + 1 夹爪

---

## 第一步：串口权限 & 固定端口绑定

每次上电后 `/dev/ttyACM*` 编号可能变化，通过 udev 规则将四条臂绑定到固定路径：

| 固定路径 | 臂 |
|---|---|
| `/dev/robot_leader_left`    | 左主臂 |
| `/dev/robot_leader_right`   | 右主臂 |
| `/dev/robot_follower_left`  | 左从臂 |
| `/dev/robot_follower_right` | 右从臂 |

### 1.1 识别各臂序列号（首次配置执行一次）

```bash
bash identify_arms.sh
```

按提示逐一拔插各臂 USB 线，脚本自动生成 `99-lerobot-arms.rules`。

### 1.2 安装 udev 规则

```bash
sudo cp 99-lerobot-arms.rules /etc/udev/rules.d/
sudo udevadm control --reload-rules && sudo udevadm trigger

# 验证绑定
ls -la /dev/robot_*
```

### 1.3 串口权限

```bash
sudo usermod -aG dialout $USER   # 重新登录后永久生效
# 或临时授权：
sudo chmod 666 /dev/robot_*
```

### 1.4 查找摄像头索引

```bash
lerobot-find-cameras opencv
```

---

## 第二步：标定四条臂

```bash
# 1. 左主臂
lerobot-calibrate \
    --teleop.type=so101_leader \
    --teleop.port=/dev/robot_leader_left \
    --teleop.id=bimanual_leader_left

# 2. 右主臂
lerobot-calibrate \
    --teleop.type=so101_leader \
    --teleop.port=/dev/robot_leader_right \
    --teleop.id=bimanual_leader_right

# 3. 左从臂
lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/robot_follower_left \
    --robot.id=bimanual_follower_left

# 4. 右从臂
lerobot-calibrate \
    --robot.type=so101_follower \
    --robot.port=/dev/robot_follower_right \
    --robot.id=bimanual_follower_right
```

---

## 第三步（可选）：测试遥操作（无摄像头）

```bash
lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port=/dev/robot_follower_left \
  --robot.right_arm_config.port=/dev/robot_follower_right \
  --robot.id=bimanual_follower \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port=/dev/robot_leader_left \
  --teleop.right_arm_config.port=/dev/robot_leader_right \
  --teleop.id=bimanual_leader \
  --display_data=false
```


---

## 第四步：测试双臂遥操作（带摄像头，不录制）

```bash
lerobot-find-cameras opencv

lerobot-teleoperate \
  --robot.type=bi_so_follower \
  --robot.left_arm_config.port=/dev/robot_follower_left \
  --robot.right_arm_config.port=/dev/robot_follower_right \
  --robot.id=bimanual_follower \
  --robot.left_arm_config.cameras='{
    left: {type: opencv, index_or_path: "/dev/video2", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --robot.right_arm_config.cameras='{
    top: {type: opencv, index_or_path: "/dev/video0", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --teleop.type=bi_so_leader \
  --teleop.left_arm_config.port=/dev/robot_leader_left \
  --teleop.right_arm_config.port=/dev/robot_leader_right \
  --teleop.id=bimanual_leader \
  --display_data=true
```

不需要摄像头时去掉 `--robot.*_arm_config.cameras` 参数，并将 `--display_data=false`。

---

## 第五步：采集双臂数据集

数据保存到本地（不上传 Hub）：

```bash
rm -rf ~/.cache/huggingface/lerobot/seeedstudio123/bimanual_test

lerobot-record \
  --robot.type=bi_so_follower \
  --robot.id=bimanual_follower \
  --robot.left_arm_config.port=/dev/robot_follower_left \
  --robot.right_arm_config.port=/dev/robot_follower_right \
  --robot.left_arm_config.cameras='{
    left: {type: opencv, index_or_path: "/dev/video2", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --robot.right_arm_config.cameras='{
    top: {type: opencv, index_or_path: "/dev/video0", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --teleop.type=bi_so_leader \
  --teleop.id=bimanual_leader \
  --teleop.left_arm_config.port=/dev/robot_leader_left \
  --teleop.right_arm_config.port=/dev/robot_leader_right \
  --display_data=true \
  --dataset.repo_id=seeedstudio123/bimanual_test \
  --dataset.num_episodes=50 \
  --dataset.single_task="双臂协作——将红色方块从左臂传递到右臂" \
  --dataset.push_to_hub=false \
  --dataset.episode_time_s=30 \
  --dataset.reset_time_s=15
```

上传到 Hugging Face Hub：

```bash
HF_USER=$(huggingface-cli whoami | head -n 1)

lerobot-record \
  --robot.type=bi_so_follower \
  --robot.id=bimanual_follower \
  --robot.left_arm_config.port=/dev/robot_follower_left \
  --robot.right_arm_config.port=/dev/robot_follower_right \
  --robot.left_arm_config.cameras='{
    left: {type: opencv, index_or_path: "/dev/video2", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --robot.right_arm_config.cameras='{
    top: {type: opencv, index_or_path: "/dev/video0", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --teleop.type=bi_so_leader \
  --teleop.id=bimanual_leader \
  --teleop.left_arm_config.port=/dev/robot_leader_left \
  --teleop.right_arm_config.port=/dev/robot_leader_right \
  --display_data=true \
  --dataset.repo_id=${HF_USER}/bimanual_test \
  --dataset.num_episodes=50 \
  --dataset.single_task="双臂协作——将红色方块从左臂传递到右臂" \
  --dataset.push_to_hub=true \
  --dataset.episode_time_s=30 \
  --dataset.reset_time_s=15
```

录制按键：
- `→`（右箭头）— 保存当前 episode，进入下一个
- `←`（左箭头）— 丢弃当前 episode，重新录制
- `ESC` — 保存并退出

> **摄像头键名必须与采集时一致**（`top`），训练和评估时都要保持相同命名，否则会报 `mean is infinity` 错误。

---

## 第六步：训练双臂 ACT 策略

本地数据集训练：

```bash
lerobot-train \
  --dataset.repo_id=seeedstudio123/bimanual_test \
  --policy.type=act \
  --output_dir=outputs/train/act_bimanual \
  --job_name=act_bimanual \
  --policy.device=cuda \
  --wandb.enable=false \
  --policy.push_to_hub=false \
  --steps=300000
```

> **说明：** 双臂动作维度为 14，数据集中已自动记录，`policy.type=act` 会从数据集元信息中自动推断输入输出维度，无需手动指定。

---

## 第七步：评估双臂策略

```bash
lerobot-record \
  --robot.type=bi_so_follower \
  --robot.id=bimanual_follower \
  --robot.left_arm_config.port=/dev/robot_follower_left \
  --robot.right_arm_config.port=/dev/robot_follower_right \
  --robot.left_arm_config.cameras='{
    left: {type: opencv, index_or_path: "/dev/video2", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --robot.right_arm_config.cameras='{
    top: {type: opencv, index_or_path: "/dev/video0", width: 640, height: 480, fps: 30, fourcc: "MJPG"}
  }' \
  --display_data=false \
  --dataset.repo_id=seeedstudio123/eval_bimanual \
  --dataset.single_task="双臂协作——将红色方块从左臂传递到右臂" \
  --dataset.push_to_hub=false \
  --policy.path=outputs/train/act_bimanual/checkpoints/last/pretrained_model
```

> - `--policy.path` 指向训练输出的最终权重目录。
> - `dataset.repo_id` 建议以 `eval_` 开头，系统会自动分目录存储评估录像。
> - 若提示 `File exists`，先删除对应的 `eval_` 目录再重新运行。

---

## 关键源码文件

| 用途 | 文件 |
|---|---|
| 双臂从臂实现 | `src/lerobot/robots/bi_so_follower/bi_so_follower.py` |
| 双臂从臂配置 | `src/lerobot/robots/bi_so_follower/config_bi_so_follower.py` |
| 双臂主臂实现 | `src/lerobot/teleoperators/bi_so_leader/bi_so_leader.py` |
| 遥操作脚本   | `src/lerobot/scripts/lerobot_teleoperate.py` |
| 数据采集脚本 | `src/lerobot/scripts/lerobot_record.py` |
| ACT 策略配置 | `src/lerobot/policies/act/configuration_act.py` |
| 训练脚本     | `src/lerobot/scripts/lerobot_train.py` |
| 端口识别工具 | `identify_arms.sh` |
| udev 规则   | `99-lerobot-arms.rules` |
